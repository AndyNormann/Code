use std::f64::consts;

pub fn offset_x(angle: f32, radius: f32) -> f32 {
    (angle / 180. * (consts::PI as f32)).sin() * radius
}

pub fn offset_y(angle: f32, radius: f32) -> f32 {
    (angle / 180. * (consts::PI as f32)).cos() * radius
}
